require "spec_helper"

describe LatestBooks do
  it "has a version number" do
    expect(LatestBooks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
